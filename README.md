# Agent demo repository

This is a small Python repository for an AI software engineering agent tutorial.
The average function works for normal lists but raises ZeroDivisionError for [].
For this project, average([]) should return 0.

Run the existing tests using `python -m pytest -q`.
They intentionally do not cover the empty list. The agent must add that test.
